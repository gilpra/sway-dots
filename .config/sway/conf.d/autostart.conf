#################
### AUTOSTART ###
#################

exec_always dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY SWAYSOCK XDG_CURRENT_DESKTOP=sway

exec mako
exec_always sh -c "pkill -x waybar; waybar"
exec_always sh -c "pkill -x autotiling; autotiling"
exec_always ~/.config/sway/scripts/battery-notify.sh

exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec wl-paste --type text --watch cliphist store
exec wl-paste --type image --watch cliphist store
